#include<bits/stdc++.h>
#include<vector>
#define ll long long int
using namespace std;
/*void spaw(ll a,ll b)
{
    ll temp=a;
    a=b;
    b=temp;}*/
int main()
{
    ll tc;
    cin>>tc;
    while(tc--)
    {
        ll n,cnt=0;
        cin>>n;
        //ll a[n];
        vector<int> v;
        for(ll i=0;i<n;i++)
        {
            ll x;
            cin>>x;
            v.push_back(x);
        }
        sort()
    }
}

